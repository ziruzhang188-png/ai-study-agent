# 🤖 AI 学习助理（有记忆的多步 Agent）

> 一位住在北京、正在学习 AI、想成为 AI 产品经理的“奶奶”，  
> 在学习过程中用 LangChain 思想亲手打造的作品级智能体 💪  

---

## 🌟 项目简介

本项目是一个具备「多步推理」与「记忆功能」的对话智能体（Agent）。  
它能根据用户输入自动决定是否调用工具（算式、日期、夸人），  
并通过文件记忆保持多轮对话的连续性。

> 🧩 本项目是《老奶奶学 Agent》学习路线中的 **方式十：作品版封装**。  
> 前九种方式涵盖 LangChain 的所有基础能力，这一版是可展示的最终成品。

---

## 🧠 功能特点

| 功能 | 说明 |
|------|------|
| 💬 多轮对话记忆 | 自动记录奶奶说过的话（保存到 `./memory/nainai_memory.txt`） |
| 🧮 多步工具调用 | 会根据语义自动算数、查日期、夸人 |
| 🧩 结构化封装 | 把 Agent 封装成可复用类 `ProductAgent` |
| 🔐 环境变量管理 | 使用 `.env` 文件读取 Key，安全又方便 |
| 🧯 容错机制 | 网络或额度异常时自动提示，不中断程序 |
| ⚙️ 可扩展性 | 可接入更多自定义工具或改造成 Web 版界面 |

---

## 🏗️ 项目结构

flowchart TD
    U[用户（奶奶）] -->|输入/多轮对话| App[ai_study_agent.py<br/>ProductAgent]
    subgraph C[Agent 内部]
        P[Persona & System Prompt<br/>（温柔助理/产品导向）]
        M[SimpleFileMemory<br/>memory/nainai_memory.txt]
        D[工具选择逻辑<br/>_need_calc / 关键词匹配]
        T1[[multiply]]
        T2[[today]]
        T3[[praise]]
        LLM[ChatOpenAI<br/>(gpt-4o)]
    end
    App --> P
    App <-.读/写历史.-> M
    App --> D
    D -->|需要算数| T1 --> App
    D -->|需要日期| T2 --> App
    D -->|夸赞| T3 --> App
    App -->|messages| LLM -->|reply| App
    subgraph Env[运行与安全]
        ENV[.env（ITEDUS_API_KEY/BASE_URL）]
        IGNORE[.gitignore（忽略 .env / memory）]
        Clean[talk_openai_direct_clean.py<br/>方式1-9 学习实验室]
    end
    ENV -.配置/加载.-> App
    IGNORE -.避免泄露.-> Repo[(Git 仓库)]
    Clean --> Repo
    App --> Repo


外部：用户和入口脚本 ai_study_agent.py
内部：Persona、记忆、工具选择、三种工具、LLM 调用
安全：.env 和 .gitignore
左练右用：talk_openai_direct_clean.py 是“学习实验室”，作品入口是 ai_study_agent.py














